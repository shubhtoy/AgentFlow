/**
 * AgentFlow Parser — Browser entry point.
 * Zero Node.js dependencies. Safe for webpack/browser bundles.
 *
 * Re-exports everything from parser-core.js which contains
 * all shared pure utilities.
 */
module.exports = require('./parser-core');
