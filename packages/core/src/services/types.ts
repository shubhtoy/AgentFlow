// Re-export from errors for convenience
export { ok, fail, ErrorCode, toServiceError } from '../errors';
export type { ServiceResult, ServiceResultOk, ServiceResultFail, ServiceError, ErrorCodeValue } from '../errors';
