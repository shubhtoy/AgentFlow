# Archived Planning Docs

Historical records from earlier project phases (pre-2026-07 doc consolidation, Epic #39).
Kept for context, **not** current. For the authoritative state see:
`docs/planning/MASTER-PLAN.md`, `docs/FEATURE-MAP.md`, `docs/DECISIONS.md`, the fumadocs
site (`studio/content/docs/`), and the GitHub project board.

Why archived: these describe superseded architecture (Agent-Spec-primary server-side export),
completed one-off phases (JS→TS rewrite, studio production hardening), or removed
implementations (the old CopilotKit/LangGraph agent graph).
