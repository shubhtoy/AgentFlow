'use client'

/**
 * AgentStateSync — renders DeepAgent activity messages in the chat.
 * In CopilotKit v2, activity messages are rendered via the CopilotKitProvider's
 * renderActivityMessages prop. This component is now a no-op since the built-in
 * activity renderer handles display automatically.
 */

export function AgentStateSync() {
  return null
}
