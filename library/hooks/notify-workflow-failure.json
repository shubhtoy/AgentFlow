{
  "name": "notify-workflow-failure",
  "description": "Alert the user immediately when a workflow fails so they can decide to retry or escalate",
  "event": "workflowFailed",
  "action": {
    "type": "notify",
    "target": "user",
    "params": {
      "message": "Workflow failed. Check the error output and decide whether to retry, debug, or escalate."
    }
  },
  "enabled": true,
  "priority": 100
}
