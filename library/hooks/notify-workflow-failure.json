{
  "name": "notify-workflow-failure",
  "version": "1.0.0",
  "description": "Notify the user when a workflow fails",
  "event": "workflowFailed",
  "action": {
    "type": "notify",
    "target": "user",
    "params": {
      "message": "Workflow failed. Check the error and decide whether to retry or escalate."
    }
  },
  "enabled": true,
  "priority": 100
}
